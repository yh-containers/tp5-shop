<?php
namespace app\admin\controller;

class Index extends Common
{

    public function index()
    {
//        dump(str_replace(['lang_var'], ['abc'] ,['shlksdfoaidf lang_var']));

//        dump(c_lang('global_title'));exit;
        return view('',[

        ]);
    }
}